<Page x:Class="MailTrayNotifier.Views.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:res="clr-namespace:MailTrayNotifier.Resources"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AboutPage">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24" VerticalAlignment="Top">
                <ui:TextBlock Text="About" FontTypography="Subtitle" Foreground="White" HorizontalAlignment="Left" Margin="0,0,0,24" />

                <ui:Card Margin="0,0,0,24">
                    <StackPanel>
                        <ui:TextBlock Text="{x:Static res:Strings.AppName}" FontTypography="BodyStrong" Margin="0,0,0,4"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                            <ui:TextBlock Text="Version: " FontTypography="Body"/>
                            <ui:TextBlock Text="{Binding AppVersion}" FontTypography="Body"/>
                        </StackPanel>
                    </StackPanel>
                </ui:Card>

                <ui:TextBlock Text="Open Source Licenses" FontTypography="BodyStrong" Foreground="White" Margin="0,0,0,8" />
                <TextBox Height="150"
                         Text="{Binding LicenseInfo, Mode=OneWay}"
                         IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto"
                         AcceptsReturn="True"
                         TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
