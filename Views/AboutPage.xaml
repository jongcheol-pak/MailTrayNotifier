<Page x:Class="MailTrayNotifier.Views.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:res="clr-namespace:MailTrayNotifier.Resources"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AboutPage">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24" VerticalAlignment="Top">
                <ui:TextBlock Text="{x:Static res:Strings.About}" FontTypography="Subtitle" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" HorizontalAlignment="Left" Margin="0,0,0,24" />

                <ui:Card Margin="0,0,0,24">
                    <StackPanel>
                        <ui:TextBlock Text="{x:Static res:Strings.AppName}" FontTypography="BodyStrong" Margin="0,0,0,4"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                            <ui:TextBlock Text="{x:Static res:Strings.VersionLabel}" FontTypography="Body"/>
                            <ui:TextBlock Text="{Binding AppVersion}" FontTypography="Body"/>
                        </StackPanel>
                    </StackPanel>
                </ui:Card>

                <ui:TextBlock Text="{x:Static res:Strings.OpenSourceLicenses}" FontTypography="BodyStrong" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,8" />
                <ItemsControl ItemsSource="{Binding OpenSourceLibraries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Button Margin="0,0,0,4"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Command="{Binding DataContext.OpenLicenseUrlCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                       CommandParameter="{Binding Url}"
                                       Appearance="Secondary">
                                <StackPanel Orientation="Horizontal">
                                    <ui:TextBlock Text="{Binding Name}" FontTypography="Body" />
                                    <ui:TextBlock Text=" (" FontTypography="Caption" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                                    <ui:TextBlock Text="{Binding License}" FontTypography="Caption" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                                    <ui:TextBlock Text=")" FontTypography="Caption" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                                </StackPanel>
                            </ui:Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
